# Production Environment Configuration
# Copy this file and fill in your production values

# Server Configuration
# Use APP_ENV instead of NODE_ENV (some platforms reserve NODE_ENV)
APP_ENV=production
PORT=3000

# Firebase Configuration
FIREBASE_DATABASE_URL=https://mr-bunkmanager-default-rtdb.firebaseio.com

# Timezone (Indian Standard Time)
TIMEZONE=Asia/Kolkata

# CORS Configuration (comma-separated allowed origins)
# Add your production app URL here
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
# Generate a strong secret for session management if needed
# SESSION_SECRET=your-secure-random-string-here

# Logging
LOG_LEVEL=info
